# Data Notes

Keep datasets and references out of Git when they are large.

Suggested future layout:

```text
data/
├── raw/
├── processed/
├── references/
└── synthetic/
```

For the first version, we will start with a tiny curated artist reference set before chasing large datasets.

